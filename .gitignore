# --- Environment / secrets ---
.env
.env.local
.env.*.local
backend/.env
frontend/.env.local

# Report automation worker state. The worker normally keeps these under
# PROGRAMDATA, but PA_WORKER_HOME can point anywhere — including inside a
# checkout. credentials.json holds the long-lived machine credential, so
# these are ignored defensively rather than relying on that never
# happening.
credentials.json
worker.pid
worker.json
worker/**/pa-copilot-*/

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
backend/.venv/
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Logs ---
*.log
backend/logs/

# --- Node / Next.js ---
node_modules/
frontend/.next/
.next/
out/
frontend/dist/
dist/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Quality measurement artefacts (regenerated every run)
backend/.quality-history.jsonl
backend/quality-report.json
backend/coverage.xml

# Local editor/agent settings — machine-specific, not project config
.claude/

# Coverage data file (the XML/lcov reports are the shareable artefacts)
backend/.coverage

# PR description drafts (working notes, not shipped)
PULL_REQUEST.md
